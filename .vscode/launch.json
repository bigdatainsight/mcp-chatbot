{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug MCP Chatbot",
            "type": "debugpy",
            "request": "launch",
            "module": "mcp_chatbot.main",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            "env": {
                "PYTHONPATH": "${workspaceFolder}/src"
            }
        }
    ]
}